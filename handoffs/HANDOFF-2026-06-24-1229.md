---
title: Session Handoff
date: 2026-06-24T12:29:00+02:00
session_id: session_01SbUkQ7HgtCFZzi4aB5P2UJ
project: memsearch (branch: qmaster-custom)
---

# Session Handoff — 2026-06-24

## Summary
No code changes to the memsearch repo. This session was (1) an upstream-divergence investigation + feasibility report for porting upstream `0.4.7→0.4.11` into the `qmaster-custom` fork, (2) a deep description of upstream's new skill-distillation feature, and (3) a cleanup of a `/handoff` skill-vs-command collision in `~/.claude`, committed to the profiles dotfiles repo. Branch was switched `main → qmaster-custom`.

## Work Completed

**Upstream divergence analysis (no files changed — findings in conversation)**
- Established the real topology: local `main` (0.4.5) is a *clean ancestor* of `upstream/main` (now 0.4.11), 0 commits ahead → trivial fast-forward, but no fork value. The fork is `qmaster-custom`: version `0.4.6`, **52 commits ahead / 35 behind** upstream. Merge-base = `ea684aa` (0.4.5).
- Fork already ported `0.4.6` (#556). Un-ported gap = `0.4.7→0.4.11`.
- `git merge-tree` (non-destructive) predicts **35 conflicting files** for a straight merge into `qmaster-custom` — far more than the `gsync` skill's stale "5 customization commits" assumption.
- Dominant upstream addition = **"Skills from Memory" / skill-distillation** (~5,900-line diff, mostly clean new files: `src/memsearch/skills.py` 501L, `transcript.py` 266L, `io.py`, `memory_to_skill.txt` ×5, `memory-to-skill/SKILL.md` ×5).

**Skill-distillation description (evidence-backed)**
- Extracted upstream source to scratchpad (see Files Modified) and described it conceptually + technically: three memory layers (episodic/semantic/procedural), candidate git store at `.memsearch/skill-candidates/`, two creation paths (background model-driven via `maintenance-runner.py` + on-demand `memsearch skills add`), the L3 `memsearch transcript` fidelity drill, and `install()` snapshot semantics.

**`~/.claude` handoff tooling cleanup (committed)**
- Diagnosed: two things named `handoff` — a *command* (`~/.claude/commands/handoff.md`, writes project `handoffs/`) and a *skill* (`~/.claude/skills/handoff`, OS-temp). The skill was shadowing the command on the `/handoff` trigger.
- Removed the skill everywhere: `.claude` symlink, untracked `.agents/skills/handoff/`, and the git-tracked `.codex/skills/handoff/`. No `handon` skill existed.
- Committed both tracked deletions to the **profiles repo** (`git profiles`, commit `a39dd6e`). `/handoff` now correctly resolves to the command (confirmed — this very handoff invoked the command template).

**Memory written**
- `live-plugin-is-frozen-cache-snapshot.md` (+ MEMORY.md pointer): the live CC plugin runs from a version-pinned cache *copy*, decoupled from the working tree.

## Dead Ends — Do Not Retry

### Reading the live plugin's state
| Approach Tried | Why It Failed | Time Spent |
|----------------|---------------|------------|
| Assumed the live plugin is statically frozen at its install SHA | It silently **re-snapshots** mid-session: `gitCommitSha` flipped `693e78c → fef2722` (current HEAD) at 11:14, likely triggered by my `claude plugin … --help` calls resyncing the `source: directory` marketplace. `version` stayed `0.4.6` the whole time. | ~10 min |
| Inspected cache at `…/0.4.6/plugins/claude-code/` and `…/0.4.6/src/memsearch/` | Empty — the cache's plugin root **is** `plugins/claude-code/` (contents are `hooks/ skills/ prompts/ podman/ transcript.py`), not the whole repo. | ~5 min |

**What to try instead**: trust `gitCommitSha` in `installed_plugins.json`, never `version`, to detect plugin drift. Plugin changes apply on **next restart**, not to the running process.

### Profiles-repo git queries
| Approach Tried | Why It Failed | Time Spent |
|----------------|---------------|------------|
| Relative pathspecs with `git profiles ls-files` | The Bash tool resets cwd to `D:\Projects\memsearch` between calls; `git profiles` uses absolute `--git-dir/--work-tree` but pathspecs are cwd-relative, giving inconsistent results. | ~5 min |

**What to try instead**: `cd /c/Users/dave` at the top of the *same* command block before any `git profiles` pathspec query.

## Key Decisions
- **Port upstream via cherry-pick pass, not gsync merge**: at 52/35 divergence the merge needs human judgment on ~17 of 35 files. A blind `gsync` would also silently auto-advance the fork-owned version line (`feedback-no-local-version-bumps`). Trade-off: more manual work, but avoids version corruption + Windows regressions.
- **Remove handoff skill, keep command**: command is Claude-Code-specific and writes to the project `handoffs/` folder the user relies on (`/handon` reads it). Skill was a cross-tool sync artifact shadowing it.
- **Removed Codex copy too** (user opted in) — leaves Codex with no handoff, accepted.

## Pending Issues
- [ ] Upstream `0.4.7→0.4.11` un-ported into `qmaster-custom` — **MED** — the `0.4.8` `memory_to_skill` distillation reintroduces the stdin→argv Windows footgun (`MAX_PROMPT_CHARS = 80_000` delivered as positional argv to native `claude.exe`, past the 32,767 `CreateProcessW` cap). See `assess-upstream-changes-windows-argv-limit`.
- [ ] `transcript.py` conflict — **MED** — fork has `plugins/claude-code/transcript.py`; upstream added `src/memsearch/transcript.py` as a core CLI command. Same concept, two homes — reconcile before merging.
- [ ] Profiles commit `a39dd6e` **not pushed** — LOW — `git profiles push` to sync `wombatfish/MyUserAIProfiles`.
- [ ] Running session still has the old `handoff` skill loaded — LOW — restart Claude Code to fully clear it (command already wins).
- [ ] Live memsearch plugin snapshot (`fef2722`) applies on next CC restart — LOW.

## Next Steps
1. **If skill-distillation is wanted**: cherry-pick port pass for `0.4.7→0.4.11` on a throwaway branch. Graft clean new files (`skills.py`, `transcript.py`, `io.py`, prompts, SKILLs), re-wire `cli.py`/`config.py`/`maintenance.py`, **re-apply the fork's stdin delivery** to `maintenance-runner.py`'s native invocation, set version line to `0.4.11` in the same commit. Use the **`gsync`** skill's remotes but NOT its merge flow.
2. **If not**: cherry-pick only #593 (Milvus upsert batching) + #44a1165 (config trust boundary); leave version at `0.4.6` as the intentional tripwire.
3. Offered earlier but not done: script the cherry-pick port pass on a throwaway branch to show the *real* (not predicted) conflict surface.

## Files Modified
- **memsearch repo**: none (only pre-existing untracked `.scratch/`, `.syndic/`, `critiques/`, `plugins/gemini/`).
- **profiles repo** (`git profiles`, commit `a39dd6e`): deleted `.claude/skills/handoff/SKILL.md`, `.codex/skills/handoff/SKILL.md`.
- **deleted untracked**: `~/.agents/skills/handoff/`.
- **memory**: `~/.claude/projects/D--Projects-memsearch/memory/live-plugin-is-frozen-cache-snapshot.md` + MEMORY.md pointer.
- **scratchpad** (read-only extracts of upstream source, not committed): `skills.py`, `transcript.py`, `memory_to_skill.txt`, `memory-to-skill-SKILL.md`, `skills-from-memory.md` under the session scratch dir.

## Context for Future Sessions
- **On branch `qmaster-custom`**, not `main`. The fork lives here; `main` is just a clean upstream mirror.
- **Fork-version policy** (`feedback-no-local-version-bumps`): version line is fork-owned and deliberately trails upstream; never bump to a version upstream lacks; the 0.4.6-vs-0.4.11 gap is an intentional tripwire. A gsync merge silently closes it — re-assert manually.
- **Windows-merge audit** is mandatory on any upstream port (`assess-upstream-changes-windows-argv-limit`, `windows-cp1252-traps`): the Linux CI never catches stdin→argv / cp1252 / POSIX-process-control regressions.
- **Shared skills registry**: `~/.agents/skills/` is read by Gemini/OpenCode/Cursor; `.claude/skills/*` are symlinks into it; Codex keeps its own real `.codex/skills/` copies (`gemini-loads-shared-agents-skills`).
- **Suggested skills next session**: `gsync` (fork remotes/sync context), `memsearch:memory-recall` (this session's findings are in memory).

## Session Feedback
Not requested (no `--feedback`).
